# Wrap then table

![](../../../assets/art/ss_points_buy_economy.png){.wrap-left width=1.6in}

This paragraph should wrap around the image. Immediately after this paragraph we start a table. The table should **not** be narrowed or shifted by the float; it should clear the float and use
the normal full text width. This paragraph should wrap around the image. Immediately after this paragraph we start a table. The table should **not** be narrowed or shifted by the float; it should clear the float and use the normal full text width. This paragraph should wrap around the image. Immediately after this paragraph we start a table. The table should **not** be narrowed or shifted by the float; it should clear the float and use the normal full text width.

| Column A | Column B |
| --- | --- |
| alpha | one |
| beta | two |
| gamma | three |

