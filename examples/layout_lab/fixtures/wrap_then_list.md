# Wrap then list

![](../../../assets/art/ss_item_carry_limit.png){.wrap-right width=1.4in}

This paragraph should wrap around the image. Immediately after this paragraph we start a list. The list should **wrap beside** the image (no forced blank gap). After the image ends, the list should naturally return to full width. This paragraph should wrap around the image. Immediately after this paragraph we start a list. The list should **wrap beside** the image (no forced blank gap). After the image ends, the list should naturally return to full width. This paragraph should wrap around the image. Immediately after this paragraph we start a list. The list should **wrap beside** the image (no forced blank gap). After the image ends, the list should naturally return to full width.

- First bullet: should align normally.
- Second bullet: should align normally.
- Third bullet: should align normally.

## After list

This heading and paragraph should not start in a narrowed column. It should clear any floats.
