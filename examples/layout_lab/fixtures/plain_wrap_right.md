# Plain wrap (right)

![](../../../assets/art/ss_die_nudge.png){.wrap-right width=1.3in}

This paragraph should wrap around the image. The image must remain **inside the text block**, and only the lines that sit alongside it should be shortened. After the image ends, the text
should return to full width. This paragraph should wrap around the image. The image must remain **inside the text block**, and only the lines that sit alongside it should be shortened. After the image ends, the text should return to full width. This paragraph should wrap around the image. The image must remain **inside the text block**, and only the lines that sit alongside it should be shortened. After the image ends, the text should return to full width.

Second paragraph: still normal flowing text. We use this to confirm that wrapping does not accidentally alter later paragraphs. Second paragraph: still normal flowing text. We use this to confirm that wrapping does not accidentally alter later paragraphs. Second paragraph: still normal flowing text. We use this to confirm that wrapping does not accidentally alter later paragraphs. Second paragraph: still normal flowing text. We use this to confirm that wrapping does not accidentally alter later paragraphs.

Third paragraph: more filler text to ensure wrapping has enough lines to demonstrate the shape. If the engine is behaving, you should see a tidy rag on the wrapped side, not a giant blank
column. Third paragraph: more filler text to ensure wrapping has enough lines to demonstrate the shape. If the engine is behaving, you should see a tidy rag on the wrapped side, not a giant blank column. Third paragraph: more filler text to ensure wrapping has enough lines to demonstrate the shape. If the engine is behaving, you should see a tidy rag on the wrapped side, not a giant blank column.

