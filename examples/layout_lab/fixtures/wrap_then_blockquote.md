# Wrap then quote

![](../../../assets/art/ss_luck_pool.png){.wrap-right width=1.6in}

This paragraph should wrap around the image. Immediately after this paragraph we start a blockquote. The quote should clear the float and render with normal margins. This paragraph should wrap around the image. Immediately after this paragraph we start a blockquote. The quote should clear the float and render with normal margins. This paragraph should wrap around the image. Immediately after this paragraph we start a blockquote. The quote should clear the float and render with normal margins. This paragraph should wrap around the image. Immediately after this paragraph we start a blockquote. The quote should clear the float and render with normal margins.

> A blockquote should not inherit a narrowed text block from a preceding float.
> If it does, the wrapping is cascading incorrectly.

Final paragraph: should render at full width. Final paragraph: should render at full width. Final paragraph: should render at full width. Final paragraph: should render at full width. Final paragraph: should render at full width. Final paragraph: should render at full width. Final paragraph: should render at full width.
