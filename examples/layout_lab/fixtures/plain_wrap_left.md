# Plain wrap (left)

![](../../../assets/art/ss_die_nudge.png){.wrap-left width=1.3in}

This paragraph should wrap around the image. The image must remain **inside the text block**, aligned to the left edge of the text area (not the page margin), and the wrap should end cleanly. This paragraph should wrap around the image. The image must remain **inside the text block**, aligned to the left edge of the text area (not the page margin), and the wrap should end cleanly. This paragraph should wrap around the image. The image must remain **inside the text block**, aligned to the left edge of the text area (not the page margin), and the wrap should end cleanly. This paragraph should wrap around the image. The image must remain **inside the text block**, aligned to the left edge of the text area (not the page margin), and the wrap should end cleanly.

Second paragraph: verify that subsequent paragraphs are not indented or otherwise affected. Second paragraph: verify that subsequent paragraphs are not indented or otherwise affected. Second paragraph: verify that subsequent paragraphs are not indented or otherwise affected. Second paragraph: verify that subsequent paragraphs are not indented or otherwise affected. Second paragraph: verify that subsequent paragraphs are not indented or otherwise affected. Second paragraph: verify that subsequent paragraphs are not indented or otherwise affected.

