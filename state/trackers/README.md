# Trackers (Private)

Session-level clocks and counters live here (pressure, scenes, optional threats).
Use tools/trackers.py for consistent updates.
The seed tracker matches templates/tracker.yaml; add extra clocks as needed.
